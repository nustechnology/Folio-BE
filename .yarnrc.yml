# Yarn 4 only supports boolean enableScripts (no per-package allowlist).
# Must stay true: @prisma/engines and esbuild need postinstall scripts.
enableScripts: true

nodeLinker: node-modules

npmMinimalAgeGate: "7d"
